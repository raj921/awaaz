# AGENTS.md

Native Indic emotional-intelligence benchmark project. Data rule that governs everything: only native-authored Hindi/Telugu data counts as evidence; no machine-translated data; no fabricated labels or consent. Read `NOTES.md` for the project history and `research/spec.md` for the research specification.

## Agent skills

### Issue tracker

Issues live as local markdown under `.scratch/<feature-slug>/` (no git remote yet). See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical triage labels, unchanged (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root (created lazily by `/domain-modeling`). See `docs/agents/domain.md`.
